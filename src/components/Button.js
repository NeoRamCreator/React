function Button(prors) {
    return (
        <div class="container">
            <button class="neon-btn neon-btn--blue">Hover me</button>
            <button class="neon-btn neon-btn--purple">Hover me</button>
            <button class="neon-btn neon-btn--green">Hover me</button>
        </div>
    )
}

export default Button;